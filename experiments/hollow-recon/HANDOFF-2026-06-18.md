# Hollow Knight (so-loader recon) → Mali-450 — HANDOFF 2026-06-18 (v3 — CENA RODA!)

> ⚠️ ESTADO DO DEVICE AO FECHAR: .88 ficou **wedgeado** (vários órfãos hollow-recon das runs
> de input cortadas pegando os 4 cores → ssh banner-timeout). Kill loop em background + o
> HK_CPULIMIT=400 de cada órfão devem matá-los; senão **power-cycle**. Ao voltar: confirmar
> `pgrep hollow-recon` (matar), rebind do gamepad (`echo 1-1:1.0 > /sys/bus/usb/drivers/usbhid/bind`),
> `systemctl start emustation`. LIÇÃO: runs de input geram órfãos — sempre timeout externo
> generoso + trap rebind, e CPULIMIT MENOR (~150) p/ auto-kill rápido.


## 🎉 TL;DR — A CENA RENDERIZA A ~25-28 FPS (resolvido!)
- 🟢 **Menu/idioma**: renderiza.
- 🟢🆕 **CENA (GameMap) RODA CONTÍNUO a ~25-28fps, ESTÁVEL, sem wedge!** (swap #2910 em
  t=102s numa run). Validado na TV ("apareceu a imagem inteira").
- 🔑 **O FIX: `HK_MAXTEX=512`** — a HK usa atlas **ASTC** 2K-4K; o Mali-450/GLES2 NÃO faz
  ASTC → a Unity DECODIFICAVA ASTC→RGBA em CPU (o "grind" que travava/wedgava o device,
  minutos). `HK_MAXTEX` reporta `GL_MAX_TEXTURE_SIZE=512` (recon_egl.c my_glGetIntegerv)
  → a Unity carrega o MIP de 512 dos atlas → ~ZERO ASTC pra decodificar → roda liso.
  (Foi a ideia do porter: "downscale o jogo inteiro pra 512 pra chegar no menu".)
- 🔴 **FALTA: CONTROLES** (input) — ver abaixo, é o muro atual.
- 🔴 Visual: fontes/quadradinhos brancos + texturas em 512 (baixa qualidade). DEPOIS do input.

## RECEITA QUE FUNCIONA (cena roda estável)
`HK_MAXTEX=512 HK_AFFINITY=3 HK_CPULIMIT=200 HK_FIXFINAL=1 HK_GLES2=1 HK_PTHREAD_SHIM=1 \
 HK_TEXCAP=512 HK_SWAPMS=20 SDL_VIDEODRIVER=mali NODRIVER=1 GC_DONT_GC=1 sh saferun.sh <s>`
- Com MAXTEX **não wedga mais** → pode rodar runs longas (sessão interativa).
- Device **<device-ip>** (DHCP; host key muda → UserKnownHostsFile=/dev/null).

## 🔴 MURO ATUAL: CONTROLES (input)
- A HK usa **Rewired** que lê `/dev/input/event*` **DIRETO** (evdev), NÃO via Unity/SDL/
  nativeInjectEvent. Por isso o `HK_AUTOKEY` (injeta KeyEvent na Unity) NUNCA navega o menu
  (o evento até chega em INJECT-PUSH, mas a UI da HK não lê esse caminho).
- A HK se vincula a **event2 = "USB Gamepad"** e reporta sempre **"Number of Controllers: 1"**.
- ⚠️ **Nem o controle FÍSICO do handheld navega** (testado: nada) — provavelmente os
  botões do handheld são **event0 (gpio_keypad)**, e a HK só lê o event2 (fantasma, sem input).
- **Tentei gamepad VIRTUAL via uinput** (`hk_pad.py` — cria "Microsoft X-Box 360 pad" em
  /dev/input/event5 com python3-evdev do device; injeta A/Start/DPAD em loop). FUNCIONA
  (event5 criado + injetando), MAS a **HK NÃO o detecta** (continua "1 controller", event2).
  → A HK auto-vincula ao event2 (menor número/primeiro gamepad) e ignora o event5.

### Deep-dive de input (sessão tarde-3) — DIAGNÓSTICO FECHADO, muro = entrega de input
- 🖼️ A tela travada é a **SELEÇÃO DE IDIOMA** (lista ~6 línguas, setas ◄► no topo; fontes
  borradas pelo MAXTEX 512). cursor roxo fixo em y=85 (topo) — ver cfb1.p0.png.
- 🔑 **fd-probe (`/proc/PID/fd`, comm=UnityMain) DEFINITIVO**: a HK abre **event0(gpio_keypad/kbd),
  event1, event3, event4, event5(meu teclado virtual!), mouse0** — e **NUNCA event2/js0 (o gamepad)**.
  Ou seja: a HK lê DEVICES DE TECLADO via evdev, mas o gamepad NÃO (esse vai por SDL/Android-API).
- ❌ **NENHUM input navega o menu** (cursor estático provado com fbcursor.py em 4-5 capturas):
  (1) teclado virtual uinput event5 — HK ABRE mas ignora as teclas (KEY_DOWN/UP/ENTER/Z/SPACE/X/C);
  (2) gamepad virtual clone (BTN_TRIGGER+ABS) — HK nem abre o event2; (3) `HK_SDLPAD=1` — idem;
  (4) nativeInjectEvent (HK_AUTOKEY tecla/gamepad) — chega em INJECT-PUSH mas menu não reage;
  (5) controle FÍSICO — nada.
- strace -f -p (5s) só pegou a main thread (não segue threads PRÉ-existentes) → não viu a thread
  de input. Pra ver os reads: atachar em CADA /proc/PID/task/* OU lançar a HK JÁ sob strace.
- 🎯 **HIPÓTESE FORTE p/ próxima sessão**: a tela de idioma da HK MOBILE é **TOUCH-only** (igual o
  EULA do NFS que "nenhum input navegava"). → injetar **MotionEvent/TOUCH** (nativeInjectEvent com
  MotionEvent, ou o caminho de toque do Unity) tocando na língua/no OK. Bogodroid (rus) tem wrappers
  jnivm de MotionEvent — referência. Alternativa: RE qual Input a UI do menu consulta (EventSystem/
  StandaloneInputModule keys vs touch vs Rewired).
- Ferramentas novas: `hk_kbd.py` (teclado virtual uinput), `run_kbd.sh` (teclado+fd-probe+strace),
  `run_ctrl.sh` (gamepad+unbind+fd-probe). 🔑 BUGFIX: comm do processo = "UnityMain" (não
  "hollow-recon") → `pkill -x hollow-recon` FALHAVA = causa dos órfãos/wedge → usar `pkill -f`.

### Deep-dive de input (sessão tarde-2) — achados CONCRETOS
- Topologia evdev do device: **event0=gpio_keypad (handler `kbd`!)** (= botões do handheld são
  TECLADO, por isso nunca navegam a HK que lê gamepad), event2="USB Gamepad" REAL (USB
  vendor 0x0810 product 0x0001, `Handlers=js0 event2`), js0=interface joystick legada.
- **Unbind/rebind do gamepad físico FUNCIONA**: `echo 1-1:1.0 > /sys/bus/usb/drivers/usbhid/unbind`
  (event2 some) e `.../bind` (volta). Usei p/ remover o real e forçar a HK no virtual.
- **`hk_pad.py` reescrito = CLONE EXATO** do "USB Gamepad" (name=" USB Gamepad          ",
  vendor 0x810/product 0x1/version 0x110, botões BTN_TRIGGER..BTN_BASE6 0x120-0x12b, eixos
  ABS_X/Y/Z/RZ/HAT0X/HAT0Y). Com o real unbound, o virtual vira event2 + **js0** (joydev cria!).
- **RESULTADO: a HK ENUMERA o virtual ("1 controller") MAS NÃO CONSOME os eventos.** Provado:
  capturei fb a cada 12s injetando só DOWN → o **cursor roxo fica FIXO em y=85** (frames
  pixel-idênticos, `tools/fbcursor.py`). Ou seja: HK vê o controle mas a navegação não reage —
  nem ao virtual, nem ao físico (confirmado). O menu É navegável (lista vertical c/ cursor
  no topo, fontes borradas 512 — ver ctrl-fb.p0.png).
- **strace ESTÁ no device** (/usr/bin/strace). PRÓXIMO PASSO DECISIVO: ver QUAL fd de input a HK
  abre e se LÊ dele — via `ls -l /proc/$(pgrep hollow-recon)/fd | grep input` COM A HK RODANDO
  (run_ctrl.sh já tenta dumpar em logs/hkfds.txt em t~38s; faltou capturar limpo por timeout).
  Hipóteses: (a) HK lê js0 e o mapeamento js de botão/eixo do meu uinput difere; (b) a thread
  de input da HK não roda/bloqueia no nosso so-loader; (c) HK usa SDL (HK_SDLPAD=1 reativa).
- ⚠️ A churn de unbind+pad+run gerou wedges/órfãos (CPULIMIT=400 demora a 25fps). Em runs de
  input: SECS curto + timeout externo GENEROSO (SECS+60) + sempre rebind no fim (trap do run_ctrl).
- Ferramentas: `hk_pad.py` (clone+inject), `run_ctrl.sh` (unbind→pad→HK→fd-probe→rebind),
  `tools/fbcursor.py` (acha linha do cursor), `tools/fbcols.py`, `tools/fb2png.py`.
3. O port RUSSO (Bogodroid, ~/Downloads/Telegram Desktop/hollow knight rus.zip, extraído em
   /tmp/hk_rus) TEM gamecontrollerdb.txt + hk.toml [input.remap] — referência de mapeamento.
   (Bogodroid usa jnivm c/ InputDevice completo — não roda no Mali-450, mas é fonte de info.)

## INFRA criada hoje (tudo via env)
- **HK_MAXTEX=N** (recon_egl.c): clampa GL_MAX_TEXTURE_SIZE → mata o decode ASTC. 🔑
- **HK_CPULIMIT=N** (main_recon.c): setrlimit RLIMIT_CPU → kernel auto-mata runaway no wedge
  (recuperação garantida; era ESSENCIAL antes do MAXTEX; manter como rede de segurança).
- **HK_AFFINITY=N** (main_recon.c): sched_setaffinity, deixa cores livres p/ o sistema.
- **HK_GFXARGS="..."** (jni_shim.c): injeta command-line da engine via getString("unity")
  (extra do Intent). (force-gfx-direct/st testados — NÃO eram a causa, mas a infra serve.)
- **HK_AUTOKEY / HK_AUTOKEY_SRC**: injeta KeyEvent (NÃO serve p/ HK — usa evdev).
- **HK_PATCHCONV**: patcha fn de conversão (NÃO usar — quebra o render; foi beco).
- **saferun.sh**: trap anti-órfão + LOGS PERSISTENTES (logs/run-<stamp>.log). hk_pad.py,
  gdbworker.sh/gdbworker2.sh (diagnóstico).
- ⚠️ Sem MAXTEX a cena wedga o device irrecuperável (power-cycle). COM MAXTEX é estável.

## Arquivos
- Fonte: src/{recon_egl,main_recon,jni_shim,pthread_shim,egl_shim}.c
- hk_pad.py (gamepad virtual uinput), saferun.sh, gdbworker*.sh
- Memória: project_hollow_knight_unity_soloader_mali450.md
